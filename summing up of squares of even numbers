#include<stdio.h>
int main()
{
  int n,i,sum=0;
  printf("enter a positive integer: ");
  scanf("%d",&n);
  for(i=2;i<=n;i+=2){
    sum +=i*i;
  }
    
    printf("the sum of the squares of the numbers from 2 to %d is:%dn",n,sum);
    return 0;
}
