#include<stdio.h>
int main()
{
  int n,i,sum=0;
  printf("enter a positive integer: ");
  scanf("%d",&n);
  for(i=1;i<=n;i+=2){
    sum +=i*i;
    
  }
  printf("the sum of the powers of the numbers from 1 to %d is:%d.\n",n,sum);
  return 0;
}
